
{% extends 'base.html.twig' %}

{% block title %}
     {{ pin.title }} | Pinterest
{% endblock %}

{% block body %}

     <article>
          <h1>{{ pin.title }}</h1>
          <p>{{ pin.description }}</p>
     </article>

     <a href="{{ path('app_pins_edit', {id:pin.id}) }}">Edit Pin</a>
     <a href="{{ path('app_home') }}">Go back</a>
{% endblock %}